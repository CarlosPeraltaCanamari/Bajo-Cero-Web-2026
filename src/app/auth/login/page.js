import LoginClient from '@/components/auth/LoginClient'

export const metadata = {
  title: 'Iniciar Sesión — Bajo Cero',
  description: 'Inicia sesión con tu CI para acceder a tus pedidos de agua premium.',
}

export default function LoginPage() {
  return <LoginClient />
}
